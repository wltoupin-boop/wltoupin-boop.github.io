# ============================================================
# Cell & Gene Therapy Tracking Platform
# Data Pipeline Dependencies
# ============================================================

# HTTP client with retry support
requests==2.31.0
tenacity==8.2.3

# Database
sqlalchemy==2.0.25
psycopg2-binary==2.9.9
alembic==1.13.1

# Anthropic AI (summaries, tagging)
anthropic==0.23.1

# GCP clients
google-cloud-secret-manager==2.18.2
google-cloud-storage==2.14.0

# Date parsing
python-dateutil==2.9.0

# Structured logging (JSON output, compatible with Cloud Logging)
structlog==24.1.0

# RSS / Atom feed parsing (FDA press releases)
feedparser==6.0.11

# Email notifications (SendGrid)
sendgrid==6.11.0

# Environment variable management
python-dotenv==1.0.1
